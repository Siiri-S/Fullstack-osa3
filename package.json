{
  "name": "puhelinluettelo_backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "del build && cd ../osa2/puhelinluettelo && npm run build && copy -r build ../../Fullstack-osa3",
    "deploy": "git push heroku main",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",    
    "logs:prod": "heroku logs --tail"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Siiri-S/Fullstack-osa3.git"
  },
  "author": "Siiri Sääksvuori",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Siiri-S/Fullstack-osa3/issues"
  },
  "homepage": "https://github.com/Siiri-S/Fullstack-osa3#readme",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.18"
  }
}
